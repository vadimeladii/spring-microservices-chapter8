spring:
  application:
    name: config-server-chapter8
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
server:
  port: 8071
